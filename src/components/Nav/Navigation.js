import NavLinks from "./NavLinks";
import './navStyles.css';

const Navigation = () => {
    return (
        <nav className="Navigation">
            <NavLinks />
        </nav>
    );
}

export default Navigation;